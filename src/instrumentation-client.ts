// Sentry client-side initialization and router transition capturing removed.
// If you have other client-side instrumentation, it can be added here.

// Example of a non-Sentry export you might keep or add:
// export const onRouterTransitionStart = (url: string) => {
//   console.log(`Client-side router transition started for: ${url}`);
// };
